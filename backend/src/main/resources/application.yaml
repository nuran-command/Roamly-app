spring:
  datasource:
    # Using the Tokyo Pooler (Port 6543) which is more firewalls-friendly
    url: jdbc:postgresql://aws-0-ap-northeast-1.pooler.supabase.com:6543/postgres?prepareThreshold=0
    username: "postgres.ckvmsfgrlxckpkaybagt"
    password: "R0NjBU0JI6NE1Yaf"
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 5
      connection-timeout: 30000
      leak-detection-threshold: 2000

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        boot:
          # Secret sauce to prevent dialect metadata detection errors
          allow_jdbc_metadata_access: false
